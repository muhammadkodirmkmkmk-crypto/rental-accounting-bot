[providers]
# Use nixpacks' built-in Python provider so pip is available on PATH
python = "3.11"

[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r telegram-bot/requirements.txt"]

[start]
cmd = "python telegram-bot/main.py"
